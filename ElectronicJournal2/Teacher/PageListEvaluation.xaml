<Page x:Class="ElectronicJournal2.Teacher.PageListEvaluation"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ElectronicJournal2.Teacher"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="PageListEvaluation">

    <Grid>

        <StackPanel Style="{StaticResource StpHorVerCen}">
            <DataGrid Name="ListEvaluation" AutoGenerateColumns="False" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"  IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="ФИО" Binding="{Binding Student.Name}"/>
                    <DataGridTextColumn Header="Оценка" Binding="{Binding Evaluation1}"/>

                </DataGrid.Columns>

            </DataGrid>

        </StackPanel>

    </Grid>
    
</Page>
